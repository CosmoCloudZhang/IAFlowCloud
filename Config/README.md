# Configuration

YAML is the authoritative source for an experiment’s data path, Conv1D
architecture, training parameters, and output directory. Relative paths are
resolved from the repository root and the resolved configuration is saved with
each run.

The active NLA baseline is `NLA/AutoEncoderConv1D.yml`.
