# Code

`IAModels` contains physical intrinsic-alignment models and scientific data
utilities. `IAFlow` contains architecture-independent compression machinery,
the implemented Conv1D network, and thin terminal entry points.

Install the repository in editable mode before using the command modules:

```bash
pip install -e .
```

The active implementation is NLA. Future model families will be introduced
with their first source files rather than placeholder directories.
