# Runs

Training artifacts are stored under `NLA/AutoEncoder/Conv1D`. Each run records
the resolved configuration, model architecture, data metadata, environment,
history, checkpoints, and validation metrics. Large artifacts remain ignored
by Git.
